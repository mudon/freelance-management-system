/api/user/client/project/quote/invoices/{invoiceId}/items
├── POST /
│     → Add invoice item
│
├── GET /
│     → Get all invoice items
│
├── GET /{itemId}
│     → Get invoice item by ID
│
├── PUT /{itemId}
│     → Update invoice item
│
├── DELETE /{itemId}
│     → Delete invoice item
│
└── POST /reorder
      → Reorder invoice items
